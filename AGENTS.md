# TrainQuiz のデータ変更

- 同形式の通常色・特別塗装を別の正解にする場合、両方を含む4択を生成し、選択肢名の意味が重ならないことを確認する。一般形式名だけの選択肢を塗装違いの誤答にしない。
- データ生成経路を変更したら、READMEのCLIから全件を一時出力へ生成して件数・ID・内容を照合する。内部のレコード生成関数の成功だけで一括再生成を完了扱いにしない。
- `data/train-sources.lock.json` は確認済みの出典と写真の記録。出典や画像指定を変えた場合は根拠を再確認し、該当する固定記録と検証履歴を更新する。ハッシュだけを更新して不一致を回避しない。元からある公開データの継承は、新たなライブ確認と区別する。
